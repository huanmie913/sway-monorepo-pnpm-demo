{
  "name": "pnpm-workspace-changsets-monorepo-example",
  "version": "0.0.0",
  "private": true,
  "description": "A monorepo workspace example with pnpm and changesets.",
  "scripts": {
    "watch": "pnpm --parallel -r run watch",
    "build": "pnpm -r run build",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "changeset publish",
    "preinstall": "npx -y only-allow pnpm"
  },
  "devDependencies": {
    "@changesets/cli": "^2.26.1",
    "rimraf": "^3.0.2",
    "typescript": "^4.9.4"
  },
  "packageManager": "pnpm@8.1.0",
  "license": "MIT"
}